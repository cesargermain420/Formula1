import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  titulo: {
    paddingTop:30,
    fontSize: 28,
    fontFamily: "F1",
    color: "#d90429",
    marginBottom: 16
  }
});